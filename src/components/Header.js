const Header = () => {
  const template = `
        <header>
            <h1>M Parcel App</h1>
            <p>This is a boilerplate app to show how to use Parcel</p>        
        </header>    
    `;

  return template;
};

export default Header;
